{
  "name": "baker-street",
  "version": "0.1.0",
  "private": true,
  "description": "Adler — open source training utility for abuse case analysis. Cases as JSON, engine as a single HTML file.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "new": "node scripts/new-case.mjs",
    "dev": "npm run build && node scripts/serve.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2T0nnks/baker-street.git"
  },
  "keywords": [
    "security",
    "privacy",
    "training",
    "threat-modeling",
    "abuse-cases",
    "secure-by-design"
  ],
  "author": "Tonnks",
  "license": "MIT",
  "devDependencies": {
    "@fontsource-variable/fraunces": "^5.3.0",
    "@fontsource/ibm-plex-sans": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^3.0.1"
  }
}
